{
  "name": "herside",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "db:push": "npx supabase db push",
    "db:types": "npx supabase gen types typescript --local > packages/supabase/types.ts",
    "db:reset": "npx supabase db reset"
  }
}
